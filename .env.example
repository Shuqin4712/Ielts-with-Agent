# 复制本文件为 .env 并填入你的值。.env 已被 .gitignore 忽略，绝不提交。

# DeepSeek API key（必填，批改/助手模式需要）
DEEPSEEK_API_KEY=

# Ollama 地址（可选，默认本地）
# OLLAMA_HOST=http://localhost:11434

# 可观测性日志（可选，默认开）：设 0 关闭 LLM 用量/延迟/成本记录
# OBS_LOG=1
