langchain-openai>=0.2
langchain-core>=0.3
langgraph>=0.2
langgraph-checkpoint-sqlite>=3.0   # 短期记忆：checkpointer 的 SQLite 后端（阶段 4）
chromadb>=0.5
ollama>=0.3
pypdf>=4.0
pymupdf>=1.24        # PDF 页栅格化（VLM gold 抽取）
python-dotenv>=1.0
tenacity>=8.2
fastapi>=0.115         # 阶段 5：后端；SSE 用其自带 StreamingResponse
uvicorn>=0.30          # 阶段 5：ASGI server（起后端）
pytest>=8.0
