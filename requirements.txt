# 直接依赖，版本已固定（== 当前验证通过的版本）。
# Python 3.13。完整传递依赖锁见 requirements.lock（pip freeze 全量）。
langchain-openai==1.3.3
langchain-core==1.4.8
langgraph==1.2.6
langgraph-checkpoint-sqlite==3.1.0   # 短期记忆：checkpointer 的 SQLite 后端
chromadb==1.5.9
ollama==0.6.2
pypdf==6.14.2
pymupdf==1.27.2.3        # PDF 页栅格化（VLM gold 抽取）
python-dotenv==1.2.2
tenacity==9.1.4
fastapi==0.139.0         # 阶段 5：后端；SSE 用其自带 StreamingResponse
uvicorn==0.49.0          # 阶段 5：ASGI server（起后端）
mcp==1.28.1              # v1.5：MCP server（把工具层暴露给 Claude Desktop 等 MCP 客户端）
pytest==9.1.1
